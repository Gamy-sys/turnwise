# --- Core (CPU-friendly, no torch required) -------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6

# ASR: CTranslate2 backend, great on CPU with int8, gives word timestamps
faster-whisper>=1.0.0

# Prosody: Praat pitch/intensity (no torch)
praat-parselmouth>=0.4.3

numpy>=1.24
scipy>=1.10
soundfile>=0.12
mido>=1.3.0
python-pptx>=1.0.0    # PowerPoint karaoke export (writes OOXML, no COM)
reportlab>=4.0.0      # PDF slide export
python-docx>=1.2.0    # Japanese four-line Word export
pykakasi>=2.3.0      # Offline Japanese → Hepburn transliteration
sudachipy>=0.6.11    # Japanese morphological word segmentation
sudachidict-core>=20260428
openai>=2.46.0        # Structured literal + natural Japanese translation

# --- Optional integrations -------------------------------------------------
# Speaker diarization + overlap (heavy, pulls in torch). See requirements-diarization.txt:
#   pip install -r requirements-diarization.txt

# Optional speaker diarization + overlap detection.
# Heavy (installs torch). CPU wheels are fine; a GPU speeds it up a lot.
# Requires a free Hugging Face token + accepting the model terms (see README).
#
# On CPU you may want the CPU-only torch wheels:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-diarization.txt

pyannote.audio>=3.1
torch>=2.1
torchaudio>=2.1
